# 2026-05-13

## Reference files read

- d:/work/k-ui/package.json — workspace root; `dev`/`build`/`docs:*` scripts run via `@k-ui/scripts`
- d:/work/k-ui/pnpm-workspace.yaml — confirms `examples/*/*` and `additional/*/*` workspaces
- d:/work/k-ui/examples/gantt/{vue3,vue2,vue-native,react18}/package.json — four framework demos exist; vue3 is the canonical one
- d:/work/k-ui/examples/gantt/vue3/{vite.config.ts,index.html,src/main.ts} — vite, dev port 8701, mounts `<DemoApp>` on `#app`
- d:/work/k-ui/examples/gantt/vue3/src/DemoApp.vue:1-2285 — the demo itself
  - 12-21 timeline-view setup and zh-CN keys
  - 23-1271 RESOURCES (~32 entries with airport/base/workshopName hierarchy + `acTypes`), CUSTOM_MARKERS, getDynamicDate helper, `generateTestEvents()` (~25 events)
  - 1273-1290 resourceAreaColumns: `airportName` (group, 35w) → `baseName` (group, 70w) → `workshopName`
  - 1291-1614 `setup()` returning `schedulerOptions`: editable+selectable+selectMirror+eventOverlap+dayMaxEvents, eventMinHeight 30, eventSpacing 10, themeOverrides, todayLine, lineStyle/markerType/dependencyLineColor, eventStyleCallbacks (textColor/bgColor/borderColor/fontSize/fontWeight), eventProcessCallback, `onLine` per-line customization, `viewDidMount` syncs `timelineView`
  - 1796-2215 `<template>`: left sidebar (use-instructions, text-color/progress/dependency docs, weekends toggle, 6-button timeline-scale group, lineStyle/markerType/lineColor/useLineEventColor controls, todayLine config, print button, currentEvents list) + main `<KGanttScheduler>` with `eventContent` slot
- d:/work/k-ui/examples/gantt/vue3/src/event-utils.ts — legacy `INITIAL_EVENTS` stub; **not used** by the demo (demo uses inline `EVENTS`)
- d:/work/k-ui/packages/gantt/package.json — core pkg `@k-ui/gantt-scheduler`, preact-based, exports `.`, `./preact`, `./internal`; iife global `KGanttScheduler`. dist/ is pre-built.
- d:/work/k-ui/additional/gantt/vue3/dist — wrapper `@k-ui/gantt-scheduler-vue3` pre-built, exposes `KGanttScheduler` Vue component + `gantt-options` types

## Mechanisms understood

- Demo is the FullCalendar-style options shape (`headerToolbar` left/center/right, `initialView`, `initialDate`, `views`, `editable`, `selectable`, `resources`, `initialEvents`, `eventResize`, `select`, `eventClick`, `eventsSet`, `viewDidMount`, `locales`/`locale`). chronix needs the same options surface to drive R2 parity.
- Six timeline scales (`day/week/month/season/halfYear/year`) are driven by built-in view presets — `views: {...}` block in the demo is commented out, so chronix must replicate the defaults, not the override paths.
- Resource area is a grouped-column tree (`resourceAreaColumns` with `group: true` on header rows); width is a percentage (`'20%'`).
- Per-event `progress` is a structured object: `{ value (0-100), backgroundColor, textColor, textFormat: '- {value}% xxx' }` rendered inside the bar.
- `eventStyleCallbacks` + `eventProcessCallback` are post-render hooks taking an EventContentArg-shaped argument (`isSelected`, `isDragging`, `event.extendedProps.*`, `backgroundColor`). Brightness-based auto-contrast in the demo at DemoApp.vue:1466-1495 is empirical; we must match the same rounding curve in goldens.
- Dependency lines: per-event `extendedProps.dependencies: string[]` declares predecessors; `onLine(line)` mutates `markerType`/`customMarker` per line at render time. `useLineEventColor` makes lines inherit start-event color; `lineStyle: 'square' | 'smooth'`.
- Today line: object `{ color, width, style: 'solid'|'dashed'|'dotted'|'double'|'dashed-dot', tooltip }` or `false`.
- **Date-anchoring**: all events go through `formatDateTime(addDays(new Date(), N), hour)` — every event is computed relative to `new Date()` at module load. For goldens this is the largest threat: identical demo bundle produces different DOM on different days. Phase 0 capture must freeze the clock (Playwright `page.clock.install({ time: '2026-05-13T08:00:00Z' })` set before navigation, or stub `Date` via init script) so the golden PNG/JSON is reproducible across runs and machines.

## Chronix work derived

- `tooling/golden-runner/` workspace package scaffolded against `http://localhost:8701/` with Playwright clock-freeze, locator-bound screenshots (sidebar excluded), and a two-spec split (`visual.spec.ts` for VRT, `interaction.spec.ts` for pointer recordings). Commits `f2a2cce` → `0533c79` → `34681e2` → `4cd637e` → `a05f918`.
- 6 visual baselines captured (`week-default` + 5 view-toggles). Half-year/year load with x-scroll at range-start → events off-screen; attempted fix via "今天" headerToolbar click failed (button disabled when today is already in range). Deferred to Phase 1.
- 2 interaction recordings: `wheel-scroll-right` (pipeline proof) and `progress-handle-drag` (highest-signal: exercises the separate-SVG-layer hit-test path that motivates `requireInitial=false` / chronix `PointerCaptureSession.requireInitialHit`).

## Additional k-ui readings for progress-handle work

- `packages/gantt/src/interaction/interactions/EventProgressing.ts:72,106-107,181,318,402-409,434-435` — `dragging.pointer.selector = '.gantt-event-progress-drag-triangle'`, instance-scoped sub-selector `[data-instance-id="${instanceId}"]`, `data-just-progress-dragged` flag set on the seg element post-drag and cleared on next tick. The pointer-up cleanup pattern matters for chronix: any in-flight transaction state needs to be torn down in the same order.
- `packages/gantt/src/interaction/interactions/EventDragging.ts:125-126` — EventDragging EXPLICITLY excludes progress-handle/triangle elements (`!elementClosest(origTarget, '.gantt-event-progress-handle')` AND `'.gantt-event-progress-drag-triangle'`). So progress drag is a parallel interaction, not a special case of event-bar drag. Two distinct chronix transactions.
- `packages/gantt/src/interaction/interactions/DateClicking.ts:52,70,77` — DateClicking also gates on the progress triangle to avoid swallowing it. Same exclusion pattern.
- `packages/gantt/src/resource-timeline/GanttView.tsx:847,1596-1598` and `packages/gantt/src/timeline/TimelineContent.tsx:180,422-424` — both views render `<g className="gantt-progress-triangles" style={{ pointerEvents: 'auto' }}>` as a separate SVG layer that re-enables hit-testing inside an otherwise pointer-events:none parent. This IS the separate-layer that drives the empirical `requireInitial=false` workaround. Chronix's IR/render must carry a "pointer-enabled overlay group" concept as a first-class layout primitive.
- `packages/gantt/src/timeline/TimelineEvent.tsx:28-30,185-260` — `progressPercent` accepts either a raw number or an object `{ value, backgroundColor, textColor, textFormat, showText }`; the rendered text uses `progressTextFormat` interpolation. Live-update during drag (not commit-on-pointer-up) is the demonstrated behavior — visible in `progress-handle-drag` mid keyframe showing 51% before pointer-up.

## Naming justifications

- For chronix-side equivalents: progress-triangle interaction = `ProgressHandleTransaction` (subset of `BarDragTransaction`'s naming family, but distinct because EventDragging explicitly excludes triangle hits — two separate transactions, not a discriminator). The CSS layer concept (the `gantt-progress-triangles` group with `pointerEvents:auto` overriding a parent) maps to chronix `PointerOverlayGroup` as a first-class IR field on render nodes.

## Open questions / parked

- Pick a frozen reference date for goldens. Candidates: `2026-05-13T08:00:00+08:00` (today, matches the demo author's `2018-02-07` reference offset of 0) or a fixed weekday-of-the-month so layouts of all 6 scales look "normal" (e.g. Wednesday mid-month). Decision to make before first capture.
- Demo also exists in vue2 (`@k-ui/gantt-vue2-example`, vite), vue-native (no dev script — pre-built only?), and react18 (`@k-ui/gantt-react18-example`, webpack serve port 8700). Plan A: capture goldens only against vue3 in Phase 0, since the underlying preact core is the same; revisit if behavior diverges between adapters during Phase 4.
- Sidebar's Chinese instructional docblocks are part of the rendered DOM. Decide whether they belong in a visual golden (they bloat diffs on any sidebar text change) or whether the VRT mask should clip them out.
- Confirm whether the demo's `dist/` artifacts are stale relative to `packages/gantt/src/`. If yes, capturing goldens against the deployed example may not reflect the source-of-truth behavior — may need a `pnpm build` cycle first.

## Additional k-ui readings for AxisRangePlanner (Phase 2 entry)

- `packages/gantt/src/DateProfileGenerator.ts:18-30` — `DateProfile` shape: `currentDate`, `validRange`, `renderRange`, `activeRange`, `currentRange`, `currentRangeUnit`, `dateIncrement`, `slotMinTime/MaxTime`. Carries the date-range partitioning used by all timeline views.
- `packages/gantt/src/timeline/timeline-date-profile.ts:26-53` — `TimelineDateProfile` shape: `labelInterval`, `slotDuration`, `slotsPerLabel`, `headerFormats`, `slotDates: DateMarker[]`, `cellRows: TimelineHeaderCell[][]`. This is the data the renderer consumes — chronix `PlannedAxis` is the analog, reorganized.
- `packages/gantt/src/timeline/timeline-date-profile.ts:55-81` — `STOCK_SUB_DURATIONS` constant: ordered list of "nice" auto-pickable durations from `years: 1` down to `milliseconds: 1`. Tuning constants `MIN_AUTO_LABELS = 18`, `MAX_AUTO_SLOTS_PER_LABEL = 6`, `MAX_AUTO_CELLS = 200`. Chronix v0 hardcodes per-view slot widths instead of auto-picking; auto-fit is Phase 2 v2.
- `packages/gantt/src/timeline/timeline-date-profile.ts:83-135` — `buildTimelineDateProfile` entry point. Calls `validateLabelAndSlot`, `ensureLabelInterval`, `ensureSlotDuration`, then resolves formatters via `createFormatter`. Multi-step assembly; chronix `defaultAxisRangePlanner.plan` is the single-function analog (v0 = day view only, throws on others).

## Chronix work derived from today's readings

- `packages/gantt/src/layout/` directory created. Contents:
  - `types.ts` — `AxisTick`, `AxisHeaderCell`, `AxisHeaderRow`, `PlannedAxis`, `ViewId`, `AxisRangePlanInput`.
  - `axis-range-planner.ts` — `AxisRangePlanner` interface and `defaultAxisRangePlanner` implementation. Day view: 24 hourly slots × 60px = 1440px total; midnight-anchored; uses `Intl.DateTimeFormat(locale, { hour: 'numeric', hour12: false })` for tick labels and full-date format for the single header row. Other five views throw "not yet implemented" — contract is wired but unrealized.
  - `axis-range-planner.test.ts` — 10 vitest assertions covering tick count, evenly-spaced x positions, single header row spanning full width, locale-driven label format, time-of-day on `anchorDate` is normalized to midnight, and 5 "throws for view X" tests for the unimplemented views.
- Commit: tbd (this session).

## Naming justifications (Phase 2 entry)

- `PlannedAxis` (vs reference `TimelineDateProfile`): names the output of a planning pass, not the data structure that drives one. The reference name conflates the input config with the computed result; chronix splits them — `AxisRangePlanInput` for the request, `PlannedAxis` for the response.
- `AxisRangePlanner.plan(input)` method-on-interface vs free `planAxisRange(input)` function: kept the interface shape because the other four layout passes will follow the same pattern (`RowSwimlaneLayout.layout(...)`, `BarPlacementPass.place(...)`, etc.), and a uniform shape lets adapters swap implementations cleanly.
