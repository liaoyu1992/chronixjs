/**
 * Creates a default empty-string value for a new DynamicInput item.
 * Adapters call this when the user clicks the "add" button.
 */
export function createDynamicInputItem(_index: number): string {
  return '';
}
