import { ChronixWave } from '@chronixjs/ui-react';

export function App() {
  return (
    <ChronixWave>
      <div style={{ padding: '24px 32px', background: '#f5f5f5', borderRadius: 8, cursor: 'pointer', display: 'inline-block' }}>
        Click anywhere for a ripple effect
      </div>
    </ChronixWave>
  );
}
