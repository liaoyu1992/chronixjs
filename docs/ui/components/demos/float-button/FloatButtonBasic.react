import { CxFloatButton } from '@chronixjs/ui-react';

export function App() {
  return (
    <div style={{ position: 'relative', height: 200, border: '1px dashed #ccc', borderRadius: 8 }}>
      <p style={{ padding: 16, color: '#666' }}>Scroll area content</p>
      <CxFloatButton shape="circle" type="primary" right={24} bottom={24} icon="plus" tooltip="Add new item" />
    </div>
  );
}
