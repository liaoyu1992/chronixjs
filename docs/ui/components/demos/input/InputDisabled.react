import { CxInput } from '@chronixjs/ui-react';

export function App() {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: 12, maxWidth: 320 }}>
      <CxInput value="正常状态" placeholder="正常输入框" />
      <CxInput value="禁用状态" disabled placeholder="禁用输入框" />
    </div>
  );
}
